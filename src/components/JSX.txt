What is JSX?

*.JSX  Standard For javascript XML
*.JSX is javascript syntax extentsion that allows you to write HTLM in React
*. JSX makes it easier to write and add HTML in React
*. JSX converts HTML tags into React elements
*. Babel is use in React for convert jsx to actual javascript
*.Components Must return black of JSX
 
